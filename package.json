{
  "name": "notification-api",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node --env-file=.env --watch --experimental-transform-types src/index.ts",
    "start": "NODE_ENV=production node --env-file=.env --experimental-transform-types src/index.ts",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "test": "NODE_ENV=test node --test --experimental-transform-types tests/**/*.test.ts"
  },
  "license": "ISC",
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "amqplib": "^0.10.9",
    "express": "^5.1",
    "http-status-codes": "^2.3.0",
    "mariadb": "^3.5.2",
    "supertest": "^7.1.4",
    "dotenv": "^17.4.2",
    "prisma": "^7.8.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/express": "^5.0",
    "@types/node": "^24.3.2",
    "typescript": "^5.9"
  }
}
